---
title: 'Carousel 轮播'
description: '横向浏览同级内容，始终提供景深动效，并支持点位插槽、自动播放、首尾循环和自定义翻页器。'
lang: zh-Hans
---

import CarouselShowcase from '@showcases/carousel';
import { ApiTable } from '@docs/rspress/api-table';

# Carousel 轮播

{"横向浏览同级内容，始终提供景深动效，并支持点位插槽、自动播放、首尾循环和自定义翻页器。"}

## 何时使用

- 同一层级有多张重点内容卡片，但当前区域只适合突出展示一项。
- 需要轮播营销亮点、版本更新或媒体内容，并允许用户主动前后浏览。

## 示例

<CarouselShowcase />

### 基础用法

{"Carousel 始终使用景深过渡；paginationPosition 控制默认点位位于内容前方或后方。"}

```tsx title="showcases/carousel/cases/basic-usage.tsx"
import { Carousel } from '@heliannuuthus/ui';

<Carousel
  controls
  items={highlights}
  pagination="dots"
  paginationPosition="after"
  renderItem={(item) => <HighlightCard item={item} />}
/>;
```

### 导航按钮

{"controls 决定是否渲染上一项和下一项按钮；按钮属性通过 previousButtonProps 与 nextButtonProps 独立扩展。"}

```tsx title="showcases/carousel/cases/navigation-buttons.tsx"
<Carousel
  controls={false}
  items={items}
  previousButtonProps={{ 'aria-label': 'Previous release' }}
  nextButtonProps={{ 'aria-label': 'Next release' }}
/>
```

### 分页点

{"pagination 控制默认点位或隐藏分页，renderDot 只改写单个点位的内容。"}

```tsx title="showcases/carousel/cases/pagination-dots.tsx"
<Carousel
  items={items}
  pagination="dots"
  renderDot={({ index, isSelected }) => (isSelected ? index + 1 : '·')}
/>
```

### 轨道与项目样式

{"classNames 按 content 与 item 语义槽位扩展轮播轨道和项目样式。"}

```tsx title="showcases/carousel/cases/track-and-item-styles.tsx"
<Carousel
  items={items}
  classNames={{ content: 'gap-3', item: 'basis-2/3 pl-3' }}
/>
```

### 自定义翻页器

{"pagination 接收函数时提供页码状态与控制方法，可直接组合符合当前页面的完整翻页器。"}

```tsx title="showcases/carousel/cases/custom-page-turner.tsx"
import { Button, Carousel } from '@heliannuuthus/ui';

<Carousel
  items={highlights}
  renderItem={(item) => <HighlightCard item={item} />}
  pagination={({ currentPage, pageCount, scrollNext, scrollPrev }) => (
    <div role="group" aria-label="轮播分页">
      <Button onClick={scrollPrev}>上一页</Button>
      <span aria-live="polite">
        {currentPage} / {pageCount}
      </span>
      <Button onClick={scrollNext}>下一页</Button>
    </div>
  )}
/>;
```

### 自动播放

{"autoplay 传 true 使用默认间隔，传数字直接设置秒数；loop 循环始终沿下一页方向越过首尾。"}

```tsx title="showcases/carousel/cases/autoplay.tsx"
import { Carousel } from '@heliannuuthus/ui';

<Carousel
  autoplay={3}
  controls
  items={highlights}
  loop
  pagination="dots"
  renderItem={(item) => <HighlightCard item={item} />}
/>;
```

### 外部控制

{"通过 ref 调用 scrollPrev、scrollNext、scrollTo、play 与 pause，不暴露底层轮播实例。"}

```tsx title="showcases/carousel/cases/external-control.tsx"
const carouselRef = useRef<CarouselRef>(null)

<Carousel ref={carouselRef} items={items} />
<Button onClick={() => carouselRef.current?.scrollPrev()}>Previous</Button>
<Button onClick={() => carouselRef.current?.scrollNext()}>Next</Button>
```

## 组成部分

- **Carousel** — 通过主组件 props 配置内容、状态、行为和扩展点。

## API

<ApiTable
  labels={{
    component: '组件',
    defaultValue: '默认值',
    description: '说明',
    name: '属性',
    type: '类型',
  }}
  rows={[
    {
      component: 'Carousel',
      defaultValue: null,
      description: '提供轮播数据或直接渲染的节点列表。',
      name: 'items',
      type: 'readonly Item[]',
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '根据当前数据项和索引渲染轮播内容。',
      name: 'renderItem',
      type: '(item: Item, index: number) => ReactNode',
    },
    {
      component: 'Carousel',
      defaultValue: 'true',
      description: '是否显示上一页与下一页按钮。',
      name: 'controls',
      type: 'boolean',
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '配置上一页按钮的外观、可访问名称和原生属性。',
      name: 'previousButtonProps',
      type: 'ButtonNativeProps',
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '配置下一页按钮的外观、可访问名称和原生属性。',
      name: 'nextButtonProps',
      type: 'ButtonNativeProps',
    },
    {
      component: 'Carousel',
      defaultValue: "'dots'",
      description: '使用默认点位、关闭分页，或通过函数自定义完整翻页器。',
      name: 'pagination',
      type: "false | 'dots' | ((controls: CarouselControls) => ReactNode)",
    },
    {
      component: 'Carousel',
      defaultValue: "'after'",
      description: '将分页内容放在轮播轨道之前或之后。',
      name: 'paginationPosition',
      type: "'before' | 'after'",
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '根据点位索引和选中状态自定义分页点。',
      name: 'renderDot',
      type: '(props: CarouselDotRenderProps) => ReactNode',
    },
    {
      component: 'Carousel',
      defaultValue: 'false',
      description:
        '传 true 以默认 3 秒间隔自动播放，或直接传入正数设置切换秒数。',
      name: 'autoplay',
      type: 'boolean | number',
    },
    {
      component: 'Carousel',
      defaultValue: 'false',
      description:
        '让最后一项与第一项首尾相接；自动播放跨越首尾时始终沿下一页方向继续。',
      name: 'loop',
      type: 'boolean',
    },
    {
      component: 'Carousel',
      defaultValue: 'true',
      description: '自动播放时，指针进入轮播区域即暂停，离开后继续。',
      name: 'pauseOnHover',
      type: 'boolean',
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '从 Carousel 外部滚动、播放或暂停；底层轮播实例不会暴露。',
      name: 'ref',
      type: 'React.Ref<CarouselRef>',
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '扩展对应语义槽位的 className。',
      name: 'classNames',
      type: 'CarouselClassNames',
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '按与 classNames 相同的语义区域设置行内样式。',
      name: 'styles',
      type: 'CarouselStyles',
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '扩展根节点样式。',
      name: 'className',
      type: 'string',
    },
    {
      component: 'Carousel',
      defaultValue: null,
      description: '扩展根节点行内样式。',
      name: 'style',
      type: 'CSSProperties',
    },
  ]}
/>

## 类型定义

### CarouselRef

```ts
type CarouselRef = {
  pause: () => void;
  play: () => void;
  scrollNext: () => void;
  scrollPrev: () => void;
  scrollTo: (index: number) => void;
};
```

### CarouselControls

```ts
type CarouselControls = CarouselRef & {
  canScrollNext: boolean;
  canScrollPrev: boolean;
  currentPage: number;
  isPlaying: boolean;
  pageCount: number;
  selectedIndex: number;
  scrollSnaps: number[];
  pause: () => void;
  play: () => void;
  scrollNext: () => void;
  scrollPrev: () => void;
  scrollTo: (index: number) => void;
};
```

### CarouselDotRenderProps

```ts
type CarouselDotRenderProps = {
  index: number;
  isSelected: boolean;
};
```

### ButtonNativeProps

```ts
type ButtonNativeProps = Omit<
  ButtonHTMLAttributes<HTMLButtonElement>,
  'color'
> & {
  variant?:
    'default' | 'outline' | 'secondary' | 'ghost' | 'destructive' | 'link';
  size?:
    'xs' | 'sm' | 'default' | 'lg' | 'icon' | 'icon-xs' | 'icon-sm' | 'icon-lg';
  block?: boolean;
  disabled?: boolean;
};
```

### CarouselClassNames

```ts
type CarouselClassNames = {
  content?: string;
  item?: string;
};
```

### CarouselStyles

```ts
type CarouselStyles = {
  content?: React.CSSProperties;
  item?: React.CSSProperties;
};
```

## 无障碍

- 轮播区域、幻灯片、前后按钮和页码点均保留可识别的语义与键盘操作。
- 开启自动播放后，动态内容不会持续触发读屏播报；系统要求减少动态效果时会停止自动播放和景深过渡。

## 避免事项

- 不要用自动播放承载必须阅读或必须操作的内容，用户仍应能通过箭头和页码点主动导航。
- 自动播放默认悬停暂停；若关闭 pauseOnHover，需要提供其他清晰的暂停方式。
- 单屏塞入过多文字会让轮播难以扫读，内容较长时改用列表或分页。
