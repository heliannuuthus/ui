---
title: 'Resizable'
description: 'Resize adjacent content areas with draggable dividers.'
lang: en
---

import ResizableShowcase from '@showcases/resizable';
import { ApiTable } from '@docs/rspress/api-table';

# Resizable

{"Resize adjacent content areas with draggable dividers."}

## When to use

- The file tree, list, or navigation needs to share the same workspace as the detail content.
- Users need to proactively allocate available space in adjacent areas based on current tasks.

## Examples

<ResizableShowcase>

```tsx title="showcases/resizable/cases/basic-usage.tsx" file="<root>/showcases/resizable/cases/basic-usage.tsx"

```

```tsx title="showcases/resizable/cases/vertical-area.tsx" file="<root>/showcases/resizable/cases/vertical-area.tsx"

```

```tsx title="showcases/resizable/cases/size-constraints-and-divider-overrides.tsx" file="<root>/showcases/resizable/cases/size-constraints-and-divider-overrides.tsx"

```

</ResizableShowcase>

## API

<ApiTable
  labels={{
    component: 'Component',
    defaultValue: 'Default',
    description: 'Description',
    name: 'Property',
    type: 'Type',
  }}
  rows={[
    {
      component: 'Resizable',
      defaultValue: null,
      description:
        'Define the content, stable identity, dimensional constraints, and divider overrides of each panel.',
      name: 'items',
      type: 'ResizableItem[]',
    },
    {
      component: 'Resizable',
      defaultValue: "'horizontal'",
      description: 'The arrangement direction of the content area.',
      name: 'orientation',
      type: "'horizontal' | 'vertical'",
    },
    {
      component: 'Resizable',
      defaultValue: null,
      description:
        'Default content for all dividers; can be passed into ReactNode, or rendered dynamically based on index and orientation.',
      name: 'separator',
      type: 'ReactNode | ((props: ResizableSeparatorRenderProps) => ReactNode)',
    },
    {
      component: 'ResizableClassNames',
      defaultValue: null,
      description: 'Styles that extend the content area of ​​all panels.',
      name: 'panel',
      type: 'string',
    },
    {
      component: 'ResizableClassNames',
      defaultValue: null,
      description: 'Extend the style of all draggable dividers.',
      name: 'separator',
      type: 'string',
    },
    {
      component: 'ResizableItem',
      defaultValue: null,
      description:
        'A stable identifier for panels, used both for React rendering and underlying layout associations.',
      name: 'key',
      type: 'string | number',
    },
    {
      component: 'ResizableItem',
      defaultValue: null,
      description: 'The React node rendered in the panel.',
      name: 'panel',
      type: 'ReactNode',
    },
    {
      component: 'ResizableItem',
      defaultValue: null,
      description:
        'Sets the initial, minimum, and maximum dimensions in order; numbers are interpreted as pixels, unitless strings are interpreted as percentages.',
      name: 'size',
      type: 'readonly [defaultSize, minSize?, maxSize?]',
    },
    {
      component: 'ResizableItem',
      defaultValue: 'false',
      description:
        'Whether to allow panel collapse when the size is below the minimum value in size.',
      name: 'collapsible',
      type: 'boolean',
    },
    {
      component: 'ResizableItem',
      defaultValue: "'0%'",
      description: 'Dimensions of the panel when folded.',
      name: 'collapsedSize',
      type: 'number | string',
    },
    {
      component: 'ResizableItem',
      defaultValue: null,
      description:
        'Overwrites the divider content after the current item; pass null to hide visual content.',
      name: 'separator',
      type: 'ResizableSeparator',
    },
    {
      component: 'ResizableItem',
      defaultValue: null,
      description:
        'Called when the panel size changes, and provides the current size, item key and last size.',
      name: 'onResize',
      type: '(size: PanelSize, key: string | number, previousSize?: PanelSize) => void',
    },
    {
      component: 'ResizableSeparatorRenderProps',
      defaultValue: null,
      description:
        'The sequential index of the current separator line among items.',
      name: 'index',
      type: 'number',
    },
    {
      component: 'ResizableSeparatorRenderProps',
      defaultValue: null,
      description: 'Stable identification of the panel before the divider.',
      name: 'itemKey',
      type: 'string | number',
    },
    {
      component: 'ResizableSeparatorRenderProps',
      defaultValue: null,
      description: 'Stable identification of a panel behind a divider.',
      name: 'nextItemKey',
      type: 'string | number',
    },
    {
      component: 'ResizableSeparatorRenderProps',
      defaultValue: null,
      description: 'The arrangement direction of the current panel group.',
      name: 'orientation',
      type: "'horizontal' | 'vertical'",
    },
    {
      component: 'Resizable',
      defaultValue: null,
      description: 'Extend the className of the corresponding semantic slot.',
      name: 'classNames',
      type: 'ResizableClassNames',
    },
    {
      component: 'Resizable',
      defaultValue: null,
      description:
        'Set inline styles using the same semantic slots as classNames.',
      name: 'styles',
      type: 'ResizableStyles',
    },
    {
      component: 'Resizable',
      defaultValue: null,
      description: 'Extend the root node style.',
      name: 'className',
      type: 'string',
    },
    {
      component: 'Resizable',
      defaultValue: null,
      description: 'Extend the root node inline style.',
      name: 'style',
      type: 'CSSProperties',
    },
  ]}
/>

## Accessibility

- Dividers remain focusable and support directional pad resizing of adjacent areas.
- Purely decorative content provided by a separator should use aria-hidden.
- Navigation and content within items still need to use their correct semantic structure.

## Avoid

- Do not let any area shrink to the point where the content cannot be understood or manipulated.
- Do not nest focusable controls such as buttons within a separator to avoid conflicting with the separator's keyboard interaction.
- When there is insufficient space on the mobile terminal, you should evaluate whether to use vertical arrangement or folding navigation instead.
